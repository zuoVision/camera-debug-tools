# Camera Debug Studio 发布验收记录

## 本次预验收（2026-08-03）

| 检查项 | 结果 | 证据 / 说明 |
|---|---|---|
| Python 版本 | PASS | Python 3.9.6，满足 Python 3.9+ |
| OpenSSH 客户端 | PASS | `/usr/bin/ssh`，OpenSSH 10.2p1 |
| Python 语法编译 | PASS | `camera_debug.py` 和 `ssh_askpass.py` 编译通过 |
| Profile JSON 解析 | PASS | demo-local、Qualcomm、BMC 全部 JSON 可解析 |
| Profile 结构校验 | PASS | 三个 Profile 均通过 `Runtime.validate_config` |
| 开发自动化测试 | PASS | 最终合并工作区执行 `sh scripts/check.sh`：20 项全部通过（2.90 秒）；隔离审计中曾出现的超时与并发断言失败未在最终门禁复现 |
| demo-local Profile | WARNING | 当前 `target.transport` 为 `ssh`，且存在被 Git 忽略的本地密码覆盖；这是现场配置，不是纯 Local 演示 |
| 拓扑指标绑定 | WARNING | Qualcomm/BMC 的 Camera 1–3、DES/SoC 和 AUX 部分连线引用未配置指标，将显示 `UNKNOWN` |
| Qualcomm 真机 | BLOCKED | 本次无真机窗口，SSH、PTY、I²C 输出解析和残留进程待人工验收 |
| BMC 真机 | BLOCKED | 本次无真机窗口，SSH、PTY、CSI I²C 输出解析和残留进程待人工验收 |
| 8 小时长稳 | BLOCKED | 需在设备窗口持续运行并采集内存、线程和子进程证据 |
| Local soak 工具 | PASS | `scripts/soak_test.py` 短时自测通过，可生成 RSS、线程、子进程、API 延迟和错误 JSON 报告；完整 8 小时仍待执行 |

结论：自动化质量门禁已经通过，但当前只完成无真机预验收，**不得标记为正式发布通过**。仍需完成至少一块真机和 8 小时长稳验收，并处理或接受上述 Profile 告警。

## 后续发布复制模板

- 版本 / Commit：
- 日期 / 执行人：
- 操作系统 / Python / OpenSSH：
- Qualcomm 板型 / 镜像：
- BMC 板型 / 镜像：

| 验收项 | 结果（PASS/FAIL/BLOCKED） | 证据 / 缺陷编号 |
|---|---|---|
| 质量门禁 |  |  |
| Local 回归 |  |  |
| Qualcomm SSH / 终端 / 监控 / 测试 |  |  |
| BMC SSH / 终端 / 监控 / 测试 |  |  |
| 配置切换与脱敏 |  |  |
| 诊断报告 |  |  |
| 8 小时长稳 |  |  |

- 未解决问题：
- 回滚方案：
- 最终结论：
